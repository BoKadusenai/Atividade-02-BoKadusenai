import math

class Circulo:

    def calculo_area(self, raio):
        area = math.pi * raio2
        print(f"A area do circulo: ",area)

    def calculo_circunferencia(self, raio):
        area = math.pi2 raio
        print(f"A circunferencia é:", area)

    def calculo_volume_cilindro(self, raio, altura):
        h = math.pi * raio2 * altura
        print(f"O volume do cilindro é:", h)

class Paralelepipedo:
    def calculo_volume(self, ladoA, ladoB, ladoC):
        volume = ladoA * ladoB * ladoC
        print(f"O volume do  paralelepipedo é: ", volume)

    def calculo_perimetro(self, comprimento, largura, altura):
        perimetro = (comprimento4) + (largura4) + (altura4)
        print(f"O perimetro do paralelepipedo: ",perimetro)

    def calculo_area_da_base(self, base, altura):
        areaBase = base altura
        print(f"A area da base do paralelepipedo é: ", areaBase)